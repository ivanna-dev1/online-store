# План Проєкту: Онлайн Магазин (React)

Цей файл містить детальний план розробки нашого навчального проєкту. Він оновлюється в процесі роботи, щоб ви могли легко продовжити з будь-якого комп'ютера.

---

## ✅ Що вже зроблено (Completed)

### 1. Налаштування та Базова Структура

- [x] Ініціалізація проєкту на Vite + React.
- [x] Налаштування TailwindCSS для стилізації.
- [x] Створено файл даних `src/products.js` з масивом товарів.
- [x] **Рефакторинг:** Всі ID товарів впорядковано по черзі (від 1 до 56).
- [x] Створено компонент `ProductCard` для відображення одного товару (картинка, назва, ціна, кнопка).

  - `/` -> Головна сторінка (HomePage з лендингом та каталогом)
  - `/cart` -> Сторінка кошика
  - `/product/:id` -> Сторінка деталей товару.

### 4. Глобальний стейт та Кошик
- [x] Реалізовано `CartContext` для управління кошиком.
- [x] Додано функції додавання, видалення та зміни кількості товарів.
- [x] Збереження кошика у `localStorage`.
- [x] **Header:** Відображення лічильника товарів та навігація.

### 5. Структура Даних та Сайдбар
- [x] **Рефакторинг:** Додано `group` та `subCategory` у `products.js`.
- [x] **Sidebar:** Створено акордеон з категоріями та підкатегоріями.
- [x] **Breadcrumbs:** Реалізовано навігаційний ланцюжок.

---

## 🚀 Що треба зробити (To Do - Next Steps)

Ми працюємо над новою Головною сторінкою (`HomePage.jsx`), яка поєднує в собі промо-слайдери та каталог.

### Етап 6: Промо-секції та Слайдери

**Крок 8**
Підготовка даних для слайдерів:
- [x] Додавання полів `onSale` та `discount` для акційних товарів.
- [x] **Оновлення:** Налаштування 8 акційних товарів з реальними фото.
- [x] **Новинки:** Додавання поля `isNew` для 10 товарів (6 з фото, 4 з заглушками).

**Крок 9 (В ПРОЦЕСІ)**
Створення та налаштування Слайдера Акцій:
- [x] Створення компонента `PromoSlider`.
- [x] Реалізація автоматичного перемикання слайдів.
- [x] Відображення ціни зі знижкою та бейджа відсотку.

**Крок 10**
Реалізація Слайдера Новинок:
- [ ] Фільтрація масиву `products` за властивістю `isNew`.
- [ ] Використання компонента `PromoSlider` для відображення новинок.
- [ ] Налаштування стилізації бейджа "New".

**Крок 11**
Фінальна збірка Головної сторінки:
- [ ] Об'єднання слайдерів та секції каталогу (з пошуком та фільтром).
- [ ] Налаштування адаптивності для мобільних пристроїв.

---

## 📂 Структура папок (Поточна)

src/
├── components/
│ ├── AllRoutes.jsx (Маршрутизація)
│ ├── Header.jsx (Верхнє меню, кошик)
│ └── ...
├── pages/
│ ├── CatalogPage.jsx (Пошук, Фільтри, Список)
│ └── CartPage.jsx (Вибрані товари)
├── App.jsx (Глобальний стейт cart)
└── products.js (Дані)
