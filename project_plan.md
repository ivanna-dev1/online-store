# План Проєкту: Онлайн Магазин (React)

Цей файл містить детальний план розробки нашого навчального проєкту. Він оновлюється в процесі роботи, щоб ви могли легко продовжити з будь-якого комп'ютера.

---

## ✅ Що вже зроблено (Completed)

### 1. Налаштування та Базова Структура

- [x] Ініціалізація проєкту на Vite + React.
- [x] Налаштування TailwindCSS для стилізації.
- [x] Створено файл даних `src/products.js` з масивом товарів.
- [x] **Рефакторинг:** Всі ID товарів впорядковано по черзі (від 1 до 56).
- [x] Створено компонент `ProductCard` для відображення одного товару (картинка, назва, ціна, кнопка).

### 2. Сторінка Каталогу (`CatalogPage`)

- [x] Реалізовано вивід списку товарів (Grid layout).
- [x] **Пошук:** Додано поле вводу (input), яке фільтрує товари за назвою.
- [x] **Фільтрація:** Додані кнопки категорій. Реалізована логіка фільтрації, яка враховує _і пошуковий запит, і обрану категорію_ одночасно.

### 3. Навігація (Routing)

- [x] Встановлено `react-router-dom`.
- [x] Налаштовано файл `App.jsx` та компонент `AllRoutes`.
- [x] Створено маршрути:
  - `/` -> Головна сторінка (Каталог)
  - `/cart` -> Сторінка кошика

---

## 🚀 Що треба зробити (To Do - Next Steps)

Ми знаходимося на етапі об'єднання логіки кошика. У `App.jsx` вже є стан кошика, але він не передається до сторінок.

### Етап 1: Підняття Стану (Lifting State Up) та Зв'язок Компонентів

**Крок 1**  
У файлі `src/App.jsx` у нас вже є стан: `const [cart, setCart] = useState([]);` та функція `addToCart`.
Нам потрібно передати ці дані (`cart`, `addToCart`) у компонент `<AllRoutes />` як пропси.

**Крок 2**  
У файлі `src/components/AllRoutes.jsx` прийміть пропси `{ cart, addToCart }`.
Передайте їх далі:

- `addToCart` та `cart` -> у `<CatalogPage />`
- `cart` (та функцію видалення, яку створимо пізніше) -> у `<CartPage />`

**Крок 3**  
Відредагуйте `src/pages/CatalogPage.jsx`:

- Видаліть заглушки `const cart = []` та `const addToCart = () => {}`.
- Прийміть реальні `cart` та `addToCart` через пропси.
- Переконайтеся, що кнопка на картці товару тепер коректно показує "In Cart", якщо товар вже додано.

### Етап 2: Header та Індикація Кошика

**Крок 4**  
У файлі `src/components/Header.jsx`:

- Додайте посилання (Link) на сторінку `/cart`.
- Відобразіть кількість товарів у кошику (наприклад, `cart.length`).
- Передайте `cart` як пропс із `App.jsx` у `Header`.

### Етап 3: Сторінка Кошика (`CartPage`)

**Крок 5**  
У `src/pages/CartPage.jsx` створіть верстку для відображення списку обраних товарів.

- Якщо кошик порожній (`cart.length === 0`), покажіть повідомлення "Кошик порожній".
- Якщо є товари, виведіть їх списком (можна використати трохи змінений `ProductCard` або простішу верстку рядка таблиці).

**Крок 6**  
Додайте функціонал видалення:

- У `App.jsx` створіть функцію `removeFromCart(productId)`.
- Передайте її через `AllRoutes` до `CartPage`.
- Додайте кнопку "Видалити" біля кожного товару в кошику.

**Крок 7**  
Підрахунок суми:

- У `CartPage` (або `App`, якщо хочете виводити це і в хедері) обчисліть загальну вартість товарів, використовуючи метод масиву `.reduce()`.
- Відобразіть "Total Price" внизу сторінки кошика.

---

## 📂 Структура папок (Поточна)

src/
├── components/
│ ├── AllRoutes.jsx (Маршрутизація)
│ ├── Header.jsx (Верхнє меню, кошик)
│ └── ...
├── pages/
│ ├── CatalogPage.jsx (Пошук, Фільтри, Список)
│ └── CartPage.jsx (Вибрані товари)
├── App.jsx (Глобальний стейт cart)
└── products.js (Дані)
