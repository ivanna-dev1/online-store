# 🗺 План проекту: Master Roadmap (Деталізований)

Цей файл є єдиним джерелом істини для проекту. (ПРИЗУПИНИЛИ РОБОТУ!)

## 🛠 Стек технологій

- **React** (Vite)
- **TailwindCSS v4**
- **React Router Dom**
- **Context API**
- **LocalStorage**

---

1_ОСНОВНИЙ ФУНКЦІОНАЛ

# 🟦 БЛОК 1: ТЕ ЩО МИ ВЖЕ ЗРОБИЛИ (Completed)

Цей розділ описує кроки, які ми вже пройшли. Використовуй цей список для відтворення проекту або перевірки логіки.

### 📦 Модуль 1-4: Базовий функціонал

- **Крок 1:** Ініціалізація проекту за допомогою Vite (`npm create vite@latest`) та очищення стандартних стилів.
- **Крок 2:** Встановлення та налаштування TailwindCSS v4 для сучасної стилізації.
- **Крок 3:** Створення бази даних у файлі `src/products.js` (масив об'єктів з id, name, price, category, image, description).
- **Крок 4:** Створення компонента `ProductCard`, який приймає дані через `props` та відображає картку товару зі стилями Tailwind.
- **Крок 5:** Створення `CatalogPage` для відображення списку всіх товарів через метод `.map()`.

### 🔍 Модуль 2: Інтерактивність та Пошук

- **Крок 6:** Додавання стану `searchTerm` (`useState`) для контрольованого інпуту пошуку.
- **Крок 7:** Реалізація динамічного списку категорій за допомогою `new Set()` з масиву товарів.
- **Крок 8:** Додавання стану `selectedCategory` для фільтрації товарів за категоріями.
- **Крок 9:** Реалізація комбінованої фільтрації: список товарів оновлюється одночасно враховуючи і пошуковий запит, і вибрану категорію.

### 🛒 Модуль 3: Глобальний стейт (Cart Context)

- **Крок 10:** Створення `CartContext` для управління кошиком по всьому додатку.
- **Крок 11:** Реалізація функцій `addToCart` (додавання товару з початковою кількістю 1).
- **Крок 12:** Додавання логіки зміни кількості: `incrementQuantity` та `decrementQuantity`.
- **Крок 13:** Створення механізму видалення товарів з кошика та перевірки `isInCart`.
- **Крок 14:** Інтеграція `localStorage` у `useEffect` для збереження стану кошика після перезавантаження сторінки.

### 🗺 Модуль 4: Навігація та Роутинг

- **Крок 15:** Встановлення `react-router-dom` та налаштування `BrowserRouter` в `main.jsx`.
- **Крок 16:** Створення компонента `AllRoutes` для визначення шляхів `/` (Catalog) та `/cart` (Cart).
- **Крок 17:** Створення компонента `Header` з навігаційними посиланнями `Link` та лічильником товарів.
- **Крок 18:** Реалізація `CartPage`: відображення обраних товарів, керування їх кількістю та підрахунок загальної суми замовлення.
- **Крок 19:** Створення сторінки `ProductDetailsPage` (`/product/:id`) з відображенням ID товару.
- **Крок 20:** Наповнення `ProductDetailsPage` реальними даними знайденого товару (картинка, опис, ціна).
- **Крок 21:** Оновлення `products.js`:
  - Додавання ієрархічної структури: поля `group` (Батьківська категорія) та `category`.
  - Заміна текстових заглушок зображень (`фото очікується`) на реальні посилання.

---

2_ДОДАТКОВИЙ ФУНКЦІОНАЛ(не переміщати і не редагувати першу частину, перед відміткою і цю відмітку)

# 🟦 БЛОК 2: ДОДАТКОВИЙ ФУНКЦІОНАЛ

### Модуль 5: Розширення каталогу та навігації

- **Крок 22:** ✓ Створення компонента `Sidebar`:
  - Відображення списку Груп (напр. "Овочі та фрукти").
  - Функціонал акордеона: відкриття підкатегорій при кліку на групу.
- **Крок 23:** ✓ Створення компонента `Breadcrumbs` (Хлібні крихти), що відображає повний шлях: `Home > Group > Category > Product`.

### 📦 Модуль 6: Нова Головна Сторінка (Landing Page)

Ми поєднуємо вітрину та каталог на головній сторінці.

- **Крок 24:** Реорганізація головної сторінки ✓:
  - Створення `HomePage.jsx` як основного лендингу.
  - Інтеграція існуючої логіки каталогу (фільтрація, пошук, список товарів) у нижню секцію `HomePage`.
  - Оновлення `AllRoutes.jsx`: головний шлях `/` веде на `HomePage`.
- **Крок 25:** Створення Слайдера 1/2 (АКЦІЇ)/(НОВИНКИ) ✓
  - 25.1: Створення файлу `recipes.js` з масивом рецептів (В ПРОЦЕСІ)
  - 25.2: Налаштування `PromoSlider` для відображення інгредієнтів.
  - 25.3: Відображення секції на HomePage.
  - **Крок 25.4:** Слайдер 3 (РЕЦЕПТИ) (опційно):
  - Додавання контенту з рецептами для покращення UX.
- **Крок 26:** Оновлення логіки Слайдера (стрілки + додавання в кошик) ✓.
- **Крок 27:** Секція "Каталог" на головній (взаємодія пошуку та категорій) ✓.

### 🔐 Модуль 7: Кабінет розробника та Авторизація (ПРОГРЕС: В РОБОТІ)

- **Крок 28: Система ролей та Вхід (Спрощено):** (В ПРОЦЕСІ)
  - 28.1: Створення файлу `LoginPage.jsx` з базовою структурою ✓.
  - 28.2: Додавання заголовка та текстового блоку "Виберіть свою роль".
  - 28.3: Налаштування маршруту `/login` у файлі `AllRoutes.jsx` ✓.
  - 28.4: Додавання посилання на сторінку входу в `Header.jsx` (ПОТРЕБУЄ КОРЕКЦІЇ).
  - 28.5: Створення кнопок-посилань для вибору ролі ("Покупець" та "Адмін") (ПОТРЕБУЄ КОРЕКЦІЇ).
  - 28.6: Реалізація функції встановлення ролі та збереження її в `localStorage` (У ПЛАНІ).
- **Крок 29: Інструментарій Розробника (Admin Tools) - ПРІОРИТЕТ:** (В РОБОТІ)
  - 29.1: Створення сторінки `AdminPage.jsx` ✓.
  - 29.2: Налаштування маршруту `/admin` ✓.
  - 29.3: Ознайомлення з `useState` для керування станами форми ✓.
  - 29.4: Створення форми додавання нового товару :
  - Автоматична генерація ID (`products.length + 1`) ✓.
  - Реєстрація обов'язкових полів (Назва, Група, Категорія, Ціна) ✓.
  - Динамічне поле знижки (з'являється при активному чекбоксі "Акція") ✓.
  - Динамічна зміна списку категорій залежно від обраної групи ✓.
  - Візуальна зона для завантаження зображень (Drag & Drop UI)✓.
  - Обмеження введення для знижки (макс. 2 символи) ✓.
  - Валідація кольорів для селектів та чекбоксів (сірий, якщо не вибрано/неактивно) ✓.
  - 29.5: Реалізація логіки збору даних (States)✓:
    - Використання **єдиного об'єктного стану** (`formData`) для всіх полів форми ✓. +++
    - Створення універсальної функції `handleChange` для оновлення полів ✓.
    - Логіка `disabled` для поля знижки, якщо чекбокс "Акція" не активний ✓.
    - Обробка зображення: використання URL або картинки-заглушки за замовчуванням ✓.
  - 29.6: Функція `handleSubmit` для додавання товару в загальний список ✓.
  - 29.7: Виведення повідомлення про успішне додавання товару ✓.
  - 29.8: Плавний скрол вгору після додавання ✓.
  - 29.9: **Екран Вибору (Admin Dashboard):** ✓
    - Створення меню: "Додати новий товар" / "Керувати списком" ✓.
    - Реалізація перемикання станів (view) в адмін-панелі ✓.
  - 29.10: **Глобальний Стейт Товарів (ProductsContext):** ✓
    - Перенесення масиву `products` у контекст, щоб товари зберігалися під час переходів між сторінками ✓.
    - Підключення `localStorage` для збереження доданих товарів після перезавантаження ✓.
  - 29.11: **Керування Асортиментом (Лист товарів):** ✓
    - Створення списку (таблиці) всіх товарів з кнопками "Редагувати" та "Видалити" ✓.
    - Можливість швидкого пошуку товару для редагування ✓.
  - 29.12: **Режим Редагування:** ✓
    - Налаштування форми для прийому даних існуючого товару ✓.
    - Функція `handleUpdate` для збереження змін у списку ✓.
  - 29.13: **Спливаюче Вікно (Preview Toast):**
    - Тимчасовий віджет з назвою та фото доданого товару для підтвердження (У ПЛАНІ).
  - 29.14: **Логіка функцій:** ✓
    - Додавання `deleteProduct` у `ProductsContext` ✓.
    - Додавання `updateProduct` у `ProductsContext` ✓.
  - 29.15: **Керування відображенням:** ✓
    - Кнопки "Редагувати Недавні" та "Редагувати Всі" ✓.
- **Крок 30: Меню Керування (Admin Dashboard):** ✓
  - 30.1: Створення стану `view` для перемикання екранів (Додати / Керувати) ✓.
  - 30.2: Додавання навігаційних кнопок для перемикання режимів ✓.
  - 30.3: Реалізація умовного рендерингу: відображення форми або списку залежно від `view` ✓.
- **Крок 31: Функції Видалення та Редагування:** ✓
  - 31.0: Створення режиму "Керування" зі списком недавніх товарів ✓.
  - 31.1: Додавання функції `deleteProduct` у `ProductsContext.jsx` (фільтрація за ID) ✓.
  - 31.2: Створення інтерфейсу списку товарів з кнопками "Видалити" та "Редагувати" ✓.
  - 31.3: Реалізація функції `updateProduct` у `ProductsContext.jsx` для збереження змін ✓.
  - 31.4: Логіка "Перед-заповнення" форми: при натисканні "Редагувати", дані товару потрапляють у `formData` ✓.
  - 31.5: Оновлення `handleSubmit`: збереження існуючого товару замість створення нового, якщо ми в режимі редагування ✓.
  - 31.6: Додавання опції "Переглянути всі товари" в режимі керування ✓.
  - 31.7: **Розширена фільтрація ✓:**
    - Оновлення об'єкта `filterOptions` для підтримки всіх критеріїв (пошук, групи, категорії, прапорці).
    - Створення універсальної функції `handleFilterChange` для оновлення фільтрів.
  - 31.8: **Верстка Сайдбару (Висувна панель) ✓:**
    - Створення фіксованої панелі зліва (`fixed left-0 top-0`).
    - Реалізація напівпрозорого фону з блюром (`backdrop-blur`).
    - Додавання кнопки "Закрити" для зручності.
  - 31.9: **Логіка Складної Фільтрації ✓:**
    - Пошук одночасно в `name` та `description`.
    - Фільтрація за динамічним діапазоном цін (`minPrice` / `maxPrice`).
    - Перевірка наявності значень у фільтрах перед порівнянням (щоб не ховати всі товари відразу).
    - Перевірка прапорців: наявність фото, описів (короткого/повного).
  - 31.10: **Динамічне оновлення ✓:**
    - Створення окремого списку категорій для фільтрації (`filterCategories`).
    - Синхронізація "живого" списку товарів з налаштуваннями фільтрів.
  - 31.11: **UI/UX полірування ✓:**
    - Реалізація кнопки "Більше / Менше" для керування списком.
    - Оптимізація виведення списку через `displayedProducts`.
  - 31.12: **Розумні чекбокси (МАЙБУТНЄ):**
    - Реалізація логіки "Так / Ні / Нейтрально" за допомогою двох взаємовиключних чекбоксів (якщо один стає True, інший автоматично стає False).
    - Оновлення логіки фільтрації для підтримки стану "Тільки НІ".

### 🛒 Модуль 8: Рефакторинг Кошика та UX

- **Крок 32:** Точний підрахунок кількості товарів (сума всіх `quantity`)✓.
- **Крок 33:** Кнопка "Оформити замовлення" (Buy) та фінальний "чек" замовлення.
- **Крок 34:** Сповіщення (Toasts) про успішні дії (додавання в кошик, успішне видалення товару).

### 📦 Модуль 9: Розширена Фільтрація та Сортування

- **Крок 35:** Фільтр за ціною (Min/Max, Range input повзунок).
- **Крок 36:** Чекбокси "Тільки в наявності", "Акційні товари".
- **Крок 37:** Сортування (від дешевих до дорогих, від А до Я).

### 🏁 Модуль 10: Полірування та Фіналізація

- **Крок 38:** Оновлення Header та Навігації: ✓
  - Додавання іконок та нових розділів: "Знижки", "Мій кабінет" (В ПРОЦЕСІ: Додано "Кабінет", 🛒) ✓.
  - Створення кнопки "Каталог" у Header для виклику сайдбару ✓.
  - Реалізація логотипу як посилання на головну ✓.
- **Крок 38.1:** Створення сторінки `ProfilePage.jsx` (Особистий кабінет).
- **Крок 39:** Покращення Sidebar ✓:
  - Реалізація перемикача (toggle) сайдбару з Header (Lifting State Up).
  - Налаштування клікабельності підкатегорій: при кліку має відбуватися фільтрація товарів у каталозі.
- **Крок 40:** Сповіщення (Toasts):
  - Випливаючі віконця "Товар додано до кошика".
- **Крок 41 (В ПРОЦЕСІ): Повна Адаптивність та Sticky UI:**
  - 41.1: Закріплення Header вгорі сторінки (`sticky top-0`) ✓.
  - 41.2: **Адаптив головного контейнера (HomePage.jsx):** Заміна фіксованих широт (w-230) на `w-full` з `max-w-` та оптимізація відступів (padding) для мобільних. +++
  - 41.3: **Мобільний лейаут слайдерів (PromoSlider.jsx):** Зміна напрямку з `flex-row` на `flex-col` всередині слайдів для вертикального відображення на телефонах.
  - 41.4: **Оптимізація контенту:** Адаптація розмірів заголовків та приховування другорядної інформації (наприклад, довгого опису) на вузьких екранах.
  - 41.5: Перевірка та виправлення вигляду всіх інших сторінок на телефонах.
  - виправити розміри w і h у всіх місцях де це може призвести до проблем з відображенням на різних пристроях.(w-1/2 - не дуже коректно писати, краще уникати чи використовувати флекс-1) +++

- **Крок 41.1:** Візуальне відображення відсутності товару:
  - Додавання стилів (блюр або напис) для товарів, де `inStock: false`.
- **Крок 42:** Пріоритизація замовлень:
  - Статуси замовлень та історія (майбутній розвиток).
- **Крок 43:** Покращення Sidebar (Lifting State Up): ✓
  - Керування відкриттям/закриттям сайдбару з Хедера ✓.
  - Реалізація фільтрації товарів при кліку на підкатегорії у Sidebar (В ПЕРСПЕКТИВІ).
- **Крок 31.13: Оптимізація коду AdminPage:** ✓
  - 31.13.1: Створення констант для початкових станів (`INITIAL_FILTERS`, `INITIAL_FORM_DATA`) ✓. +++
  - 31.13.2: Оптимізація `isFiltering` через `Object.entries().some()` ✓. +++
  - 31.13.3: Спрощення логіки розрахунку `displayedProducts` ✓. +++
- **Крок 31.14: Рефакторинг та Розподіл на компоненти (ПРІОРИТЕТ):** +++
  - Винесення форми додавання товару в `AdminProductForm.jsx`.
  - Винесення списку товарів з фільтрацією в `AdminProductList.jsx`.
- **Крок 31.15: Оптимізація продуктивності (useMemo):**
  - Використання `useMemo` для списків `groups` та `categories`.
- **Крок 31.16: Уніфікація UI Фільтрів:**
  - Оновлення стилів фільтрів в адмін-панелі за аналогією до Сайдбару каталогу.
- **Крок 31.17: **Інтеграція Каталогу (НАСТУПНИЙ ПРІОРИТЕТ):\*\*
  - 31.17.1: Зв'язати кліки в `Sidebar.jsx` з фільтрацією в `CatalogPage.jsx` (Lifting State Up).
  - 31.17.2: Реалізація клікабельності кожної ланки в `Breadcrumbs.jsx`.
  - 31.17.3: Перевірка взаємодії пошуку та кліків по категоріях.
- **Крок 44:** Покращення Breadcrumbs (кліабельність усіх ланок шляху).
- **Крок 45:** Інтерактивні стрілки в Sidebar для розгортання категорій.

### 📦 Модуль 11: Керування замовленнями (Admin Orders)

- **Крок 46:** Створення вкладки "Замовлення" в Адмін-панелі.
- **Крок 47:** Збереження історії покупок у `localStorage` як список "Замовлень".
- **Крок 48:** Відображення списку замовлень (дата, товари, сума) для адміністратора.

### 🚀 Модуль 12: SEO та Доступність

- **Крок 49:** Додавання `meta title` та `description` для кожної сторінки.
- **Крок 50:** Перевірка доступності (Alt-теги для фото, семантика HTML).
- дописати заглушки(текст, картинки) всюди де можна (де може пропасти контент) +++

### 🏁 Модуль 13: Фіналізація та Деплой

- **Крок 51:** Фінальне тестування всіх функцій (кошик, фільтри, адмінка).
- **Крок 52:** Оптимізація зображень та видалення зайвого коду.
- **Крок 53:** Збірка проекту (`npm run build`) та поради щодо публікації (Vercel/Netlify).

---

## 💡 Нотатки для навчання (Learning Notes)

- **Nested Objects**: Робота зі складними масивами (групування товарів за категоріями).
- **Conditional Rendering**: Керування екранами адмінки через стан `view`.
- **Lifting State Up**: Перенесення стану до спільного батька (напр. Header -> Sidebar).
- **URL Parameters**: Використання `useParams` для сторінок товарів.

---

## 📝 Додаткові правила та зноски

### 📋 Технічні правила заповнення даних (на вимогу):

_Ці правила застосовуються тільки при масовому оновленні або за запитом користувача:_

- **isNew**: `true` для кожного 6-го товару.
- **onSale**: `true` для кожного 4-го товару.
- **inStock**: `false` для кожного 13-го товару, **АЛЕ** якщо цей товар вже є акційним (`onSale: true`) або новинкою (`isNew: true`), то він залишається в наявності (`inStock: true`), а статус `false` переноситься на наступний за ним товар.

---

# 📊 Аудит проекту та Технічний аналіз

## 1. Аналіз існуючого плану та функціональні прогалини

Поточний план проекту `Online Store` дуже детальний і охоплює всі базові потреби: каталог, кошик з логікою кількості, адмін-панель з повним CRUD та складну фільтрацію. Ви вже реалізували архітектурно правильні речі, такі як `Context API` для товарів та кошика, що є чудовим фундаментом.

**Що можна доповнити:**

- **Повноцінна авторизація:** Зараз вибір ролі спрощений. Наступним кроком може бути інтеграція з цікавими сервісами на кшталт `Firebase Auth`.
- **Процес оформлення замовлення (Checkout):** Створення форми з валідацією (ім'я, телефон, адреса доставки) та підсумкової сторінки успіху.
- **Відгуки та рейтинги:** Користувачі більше довіряють товарам з "зірочками" та реальними відгуками.
- **Особистий кабінет:** Сторінка з історією замовлень та збереженими даними користувача.

## 2. Приклади фіч з популярних маркетплейсів (Amazon, Rozetka)

- **"З цим товаром також купують":** Розумний блок рекомендацій на сторінці товару.
- **Список бажань (Wishlist):** Можливість відкласти товар "на потім" без додавання в кошик.
- **Порівняння товарів:** Зручно для техніки або фруктів (за калорійністю/ціною), коли можна порівняти характеристики.
- **Живий пошук:** Автодоповнення та підказки товарів прямо під час введення тексту в пошук.

## 3. Технічні покращення та оптимізація

- **Валідація форм:** Використання бібліотек на кшталт `React Hook Form` для складних форм в адмінці, щоб зменшити кількість перерендерів.
- **Оптимізація зображень:** Використання сучасних форматів (WebP) та `Lazy Loading`, щоб сайт завантажувався миттєво.
- **Хмарне зберігання:** Перехід від `localStorage` до реальної БД (наприклад, `Supabase` або `Firebase`), щоб дані були доступні з будь-якого пристрою.

## 4. UX/UI фічі для залучення користувачів

- **Темна тема:** Сучасний стандарт, що покращує комфорт користування ввечері.
- **Skeleton Screens:** Візуальні заповнювачі, які показуються замість спінерів під час завантаження товарів.
- **Мікро-анімації:** Додавання плавних переходів при додаванні товару в кошик (наприклад, через `Framer Motion`).

---

# ⏳ Орієнтовний час на реалізацію (Time Estimates)

Нижче наведено приблизний час для впровадження нових частин проекту, враховуючи навчальний темп:

| Частина проекту                              | Складність | Орієнтовний час |
| :------------------------------------------- | :--------- | :-------------- |
| **Система авторизації (Firebase/Mock дані)** | Середня    | 2-3 дні         |
| **Оформлення замовлення (Checkout Flow)**    | Середня    | 1-2 дні         |
| **Особистий кабінет та історія замовлень**   | Середня    | 2 дні           |
| **Система відгуків та рейтингів**            | Низька     | 1 день          |
| **Розумний пошук та рекомендації**           | Висока     | 1-2 дні         |
| **Темна тема (Dark Mode)**                   | Низька     | 0.5 - 1 день    |
| **SEO оптимізація та фінальний деплой**      | Низька     | 1 день          |
