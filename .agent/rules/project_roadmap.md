# Прогрес навчання: React Онлайн Магазин

Цей документ містить актуальний стан проекту, виконані завдання та план подальших дій.
Файл оновлюється автоматично по мірі проходження курсу.

## 🛠 Технології

- **React 18/19** (Vite)
- **TailwindCSS v4** (Стилізація)
- **React Router Dom** (Навігація)
- **Context API** (Управління станом)

---

## ✅ Вже зроблено (Completed)

### Модуль 1: Основи та Дані

- [x] Ініціалізація проекту (Vite + React).
- [x] Налаштування TailwindCSS v4.
- [x] Створення масиву товарів (`products.js`).
- [x] Компонент `ProductCard` (відображення одного товару).
- [x] Рендеринг списку товарів у сітку (Grid).

### Модуль 2: Інтерактивність

- [x] Пошук товарів (контрольований input).
- [x] Фільтрація за назвою (case-insensitive).
- [x] Фільтрація за категоріями (динамічні кнопки).
- [x] Комбінована фільтрація (Пошук + Категорія).

### Модуль 3: Кошик (Lifting State Up -> Context API)

- [x] Створення стану кошика.
- [x] Функція додавання товару.
- [x] Відображення лічильника товарів.
- [x] Візуалізація стану "В кошику" (зміна кнопки, блокування).
- [x] **Refactoring:** Перехід на Context API (`CartContext`).
  - [x] Створено `CartProvider`.
  - [x] Підключено в `main.jsx`.
  - [x] Використання `useCart` в компонентах (`Header`, `CatalogPage`).

### Модуль 4: Структура та Роутинг

- [x] Встановлення `react-router-dom`.
- [x] Розділення на сторінки (`CatalogPage`, `CartPage`).
- [x] Налаштування маршрутизації в `App.jsx` (або `AllRoutes.jsx`).

---

## 🚀 Поточний етап (Current Task)

**Модуль 5: Сторінка Кошика**
Зараз у нас є порожня сторінка `/cart`. Нам потрібно наповнити її функціоналом.

---

## 📋 План на майбутнє (To Do)

### Модуль 5: Сторінка Кошика

1. **Відображення списку:**
   - Отримати `cart` з контексту.
   - Вивести список обраних товарів (таблиця або список карток).
   - Показати повідомлення, якщо кошик порожній.
2. **Управління:**
   - Функція видалення товару (`removeFromCart`).
   - (Опціонально) Зміна кількості (`quantity` +/-).
3. **Підсумки:**
   - Підрахунок загальної суми (`total price`).

### Модуль 6: Покращення UX/UI

1. **Навігація:**
   - Додати посилання (Link) у Header для переходу між Каталогом та Кошиком.
2. **Збереження даних:**
   - Використання `localStorage`, щоб кошик не зникав після оновлення сторінки.
3. **Деталі товару (Опціонально):**
   - Сторінка окремого товару (`/product/:id`).

---

## 💡 Корисні нотатки (Notes)

- **Context API** дозволяє уникнути передачі пропсів через багато рівнів (Prop Drilling).
- **Derived State** (похідний стан) використовується для фільтрації, щоб не змінювати оригінальний масив даних.
- **Controlled Components** (інпути) — значення інпуту завжди синхронізоване зі стейтом React.
