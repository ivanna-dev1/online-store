---
trigger: always_on
---

я джуніор початківець вивчаю js, зараз вивчаю ріакт
Навички:
React
Функціональні компоненти, props
useState, useEffect, мінімально useMemo / useCallback, useRef
Форми, умовний рендеринг, списки
Масиви (map, filter, reduce)
Об’єкти
Робота з CSS бібліотеками(tailwindcss)

Реалізовані проєкти
панель навігації
Footer
Profile card
Error / CTA / Pricing компоненти(тільки tailwindcss без функціоналу)
Color picker
Mood board
Password generator
Пошук фруктів через API
Shopping list
RSVP форма
Анкета користувача
Конвертер валют
Tic-Tac-Toe

Майже не вивчено
Оптимізація продуктивності
Архітектура додатків
Глобальний state
Кастомні хуки
Складні API
Тестування
Автотестування
Великі state-менеджери
Production-архітектура

я хочу зробити навчальний проєкт з нуля: Онлайн магазин
Каталог + фільтрація + пошук + корзина(повинна бути окремою сторінкою)
я проходила навчання в програмі фрі куд кемп і там навчання організоване по такій схемі(хочу щоб ти зробив схоже):  
 Крок 1 У попередніх уроках ви дізналися про обробку побічних ефектів, таких як отримання даних, за допомогою useEffectхука. У цьому семінарі ви створите застосунок, який отримує дані про фрукти з API на основі введених користувачем даних та динамічно відображає результати. Спочатку зробіть так, щоб ваш FruitsSearchкомпонент повертав divелемент idз search-container.
Крок 2 Далі ви створите форму, де користувачі зможуть шукати фрукти. Всередині #search-containerелемента створіть formелемент.
Крок 3 Всередині formелемента додайте labelелемент. Ваш labelелемент повинен містити текст Search for fruits:та htmlForатрибут зі значенням "search-input".
Крок 4 Всередині formелемента, після мітки, додайте вхідні дані з an idof search-inputта a typeof search.
Крок 5 У React контрольовані вхідні дані є стандартним способом обробки даних форми, де значення вхідних даних синхронізується зі станом компонента. const MyForm = () => { const [name, setName] = useState(""); return ( <> <label htmlFor="name">Your name</label> <input type="text" value={name} id="name" /> </> ); } Ви дізнаєтеся більше про цю закономірність на наступному уроці. Щоб відстежувати, що користувач вводить у поле пошуку, створіть змінну стану queryз початковим значенням порожнього рядка ( ''). Також визначте відповідну функцію встановлення , setQuery, за допомогою useStateхука.
Крок 10 Передайте handleSubmitфункцію до onSubmitвластивості елемента form, щоб надсилання форми могло бути оброблено правильно.
Крок 11 Після форми додайте divелемент з , idякий resultsвідображатиме результати пошуку.
Крок 12 Всередині #resultsелемента використовуйте тернарний оператор, щоб перевірити, чи довжина resultsмасиву більша за нуль. Якщо так, відобразіть елементи та відобразіть кожен з них в pелементі з класом . Також result-itemобов'язково додайте атрибут до кожного абзацу.key Якщо resultsвоно порожнє, відобразити pелемент із повідомленням No results found.

План проекту збережений в реплзиторії. Продовжуемо за планом.
До кожного нового проекту пиши детальний план роботи і редагуй його в процесі нашої роботи(що ми вже зробили що ще треба зробити, або якщо ми змінили план роботи чи вектор роботи)
